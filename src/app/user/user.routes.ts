import { Routes } from '@angular/router';

export const userRoutes: Routes = [
  // Exemplo de rota inicial, pode ser expandida conforme novos componentes
  // { path: 'perfil', component: UserProfileComponent },
]; 